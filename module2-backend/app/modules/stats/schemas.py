"""Stats schemas — responses are documented in API-SPECIFICATION.md Statistics."""

from pydantic import BaseModel


class StatsPlaceholder(BaseModel):
    """Typed responses can be added as you implement each /stats endpoint."""
